<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/screen"
    android:orientation="vertical"
    android:padding="@dimen/generalPadding"
    tools:ignore="Overdraw">

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="@dimen/dialogBoxMargin"
        android:gravity="center"
        android:textSize="@dimen/txtTitle" />

    <ImageButton
        android:id="@+id/ibCorrectFlag"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/dialogBoxMargin"
        android:background="@android:color/transparent"
        android:paddingEnd="@dimen/boxLengthPadding"
        android:paddingLeft="@dimen/boxLengthPadding"
        android:paddingRight="@dimen/boxLengthPadding"
        android:paddingStart="@dimen/boxLengthPadding"
        tools:ignore="ContentDescription" />

    <Button
        android:id="@+id/btnNextQuestion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/dialogBoxMargin"
        android:background="@color/colorPrimary"
        android:padding="@dimen/generalPadding"
        android:textColor="@color/screen"
        android:textSize="@dimen/btnDialogBox" />
</LinearLayout>